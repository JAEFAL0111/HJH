#include<iostream>
using namespace std;

int main(void)
{니
	cout << "주소를 입력하세요";

	char address[100];
	cin.getline(address, 100, '\n');  // '\n' 엔터를치면 그 전까지 입력받고 종료. 입력 안할 시, 디폴드값으로 저장
	cout << "주소는 " << address << "입니다\n";
}
